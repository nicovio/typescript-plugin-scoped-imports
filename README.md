# typescript-plugin-scoped-imports workspace

Monorepo del plugin `typescript-plugin-scoped-imports`.

## Estructura

- `packages/typescript-plugin-scoped-imports`: paquete publicable del plugin
- `examples/test-project`: proyecto consumidor para validar comportamiento real

## Comandos

```bash
pnpm install
pnpm run typecheck
pnpm run build
```

## Desarrollo del plugin

La documentacion del plugin esta en:
`packages/typescript-plugin-scoped-imports/README.md`
